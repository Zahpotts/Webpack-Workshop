import React from "react";
import ReactDOM from "react-dom";

const rootElement = document.getElementById("root");

const sampleCompoent = <div>ItS WORKING</div>;
ReactDOM.render(sampleCompoent, rootElement);